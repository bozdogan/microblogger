
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand d-flex w-50 me-auto" href="/">
      <img class="" height="48" src="/res/wp-brand.png">
      <span class="brand fs-4 ms-1 my-auto">Wallposter</span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse w-100" id="navbarNav">
      <ul class="nav navbar-nav w-100 justify-content-center text-center">
        <li class="nav-item">
          <a class="nav-link <% if(locals.activePage === 'posts') { %>active<% } %>" href="/posts">See the Wall</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <% if(locals.activePage === 'new-post') { %>active<% } %>" href="/posts/new">Send a Post</a>
        </li>
      </ul>
<% if(!locals.user) { %>
      <ul class="nav navbar-nav ms-auto w-100 justify-content-end text-end">
        <li class="nav-item">
          <a class="nav-link <% if(locals.activePage === 'login') { %>active<% } %>" href="/account/login">Login</a>
        </li>
      </ul>
<% } else { %>
      <ul class="nav navbar-nav ms-auto w-100 justify-content-end text-end">
        <li class="nav-item">
          <div class="small">
            <span class="text-muted">Logged in as</span>
            <span class="text-info"><%= user.username %></span>
          </div>
          <div class="">
            <a class="text-muted logout" href="/account/logout" title="Log Out">
              Log out
            </a>
          </div>
        </li>
      </ul>
<% } %>
    </div>
  </div>
</nav>
